apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sensors.fullname" . }}-test-connection"
  labels:
    {{- include "sensors.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sensors.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
